package opentree.synthesis.filtering;

import org.neo4j.graphdb.Relationship;

/**
 * Deprecated when adding filter directives. Will probably never be reinstated.
 * @author cody
 *
 */
@Deprecated
public interface FilterCriterionOLD {
	/*
	
	/**
	 * Returns true if the relationship passes the filter, and false if it does not.
	 * @param r
	 * @return
	 *
	abstract boolean validate(Relationship r);
	abstract String getDescription();
	abstract String getReport();
*/	
}
