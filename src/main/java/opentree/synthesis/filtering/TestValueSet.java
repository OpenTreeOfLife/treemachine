package opentree.synthesis.filtering;

import java.util.HashSet;

/**
 * Never finished, because it is never used, and probably never needs to be.
 * @author cody
 *
 * @param <T>
 */
@Deprecated
public class TestValueSet<T>  {
/*
	HashSet<String> h;
	Class<?> type;
	
	public TestValueSet(HashSet<String> h){
		this.h = h;
		this.type = HashSet.class;
	}

	public boolean contains(SourcePropertyValue comparable) {
	
		if (comparable.type() != type) {
			throw new java.lang.UnsupportedOperationException(illegalComparisonMessage(comparable.type()));

		} else {
			return h.contains(comparable.value());
		}
	}
	
	private String illegalComparisonMessage(Class<?> incomparableClass) {
		return "cannot compare " + incomparableClass.toString() + " to " + this.type.toString();
	} */
}
